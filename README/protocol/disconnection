1、概述：
  disconnection(断开连接包)，设备或APP建立该包发往服务器来断开与服务器的连接。服务器接收到该包后将释放为设备或APP分配的上下文，并断开与其的TCP SOCKET连接。


2、建立包格式：
  断开连接包由设备或APP建立，包的信息以JSON格式组织，设备与APP建立的断开连接包的格式是一样的，下面列举该包的格式：
  {
    "cmd":"disconnection"                  // 命令字段：disconnection指明是断开连接包
  }


3、返回包格式：
  对于断开连接包，服务器不返回任何包
